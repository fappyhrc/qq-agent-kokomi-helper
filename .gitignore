# kokomi-helper · 版本库排除清单

# ── 运行期数据 ───────────────────────────────────────────────────────────────
# 图片暂存走系统临时目录（见 lib/image-store.js），插件目录内只留日志位
log/
*.log

# 查询凭据窗口落在核心 DATA_DIR（默认 data/，多实例时是 data-2/ 等）。
# 与插件同目录部署时它可能落在插件目录内，因此这里也排一遍。
kokomi-claim.json
data/

# ── 本地临时文件 ─────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.cm*.txt
COMMIT_MSG.txt

# 说明：本插件是纯 Node 实现，没有 .venv / .kokomi-src / 渲染依赖需要忽略。
# 早期版本曾 vendor Kokomi_Bot v4.1.1（365 MB）并建私有 venv（196 MB），
# 改直连模式后已全部删除，见 DEVELOPMENT.md §1.2。